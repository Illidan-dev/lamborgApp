import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    priceLabelContainer: {
        flexDirection: 'row',
        justifyContent: 'space-around',
        alignItems: 'center',
        padding: 10,
        marginTop: 10,
    },

    priceLabel: {
        color: '#fff',
        fontSize: 22,
    }
}); 