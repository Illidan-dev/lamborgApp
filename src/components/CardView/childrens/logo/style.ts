import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    logoContainer: {
        width: '100%',
        height: 200,
        resizeMode: 'contain',
        alignItems: 'center',
        justifyContent: 'center',
    },
    logoImage: {
        flex: 1,
        width: '65%',
        resizeMode: 'contain'
    },
})