import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    carBrand: {
        color: '#fff',
        fontWeight: '400',
        fontSize: 18,
        fontStyle: 'italic'
    },

    carName: {
        color: '#fff',
        fontSize: 20,
        fontWeight: 'bold',
        textAlign: 'center'
    },
});